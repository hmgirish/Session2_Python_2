1. Write a program which accepts a sequence of comma-separated numbers from console and
generate a list.

Solution:

values = input("Enter a sequence of numbers separated by commas: ")
l = values.split(",")

print ('The list of numbers are: ', l)
